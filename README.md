# AIMC - AI Minecraft Agent

基于 **STEVE-1** 模型的 Minecraft AI Agent 项目。通过评估和优化 STEVE-1 模型，实现能够理解自然语言指令并在 Minecraft 中执行复杂任务的智能代理。

## 🎯 项目目标

- **短期目标**：建立完整的 STEVE-1 模型评估体系，分析模型在各类任务上的表现
- **中期目标**：基于评估结果，针对性地优化和微调模型
- **长期目标**：构建能够执行复杂 Minecraft 任务的通用 AI Agent

## ✅ 当前进度

### 已完成
- ✅ 完整的评估框架（支持 Prior、动作相似度、Policy 执行三阶段评估）
- ✅ 25+ 预定义评估任务（采集、战斗、合成等类别）
- ✅ 训练样本录制工具（自动生成视觉嵌入）
- ✅ HTML 评估报告生成
- ✅ 检查点机制（支持断点续评）
- ✅ 模型复用优化（批量评估时模型只加载一次）

### 进行中
- 🔄 分析评估结果，确定模型提升方向
- 🔄 设计训练策略调整方案

## 📁 项目结构

```
aimc/
├── config/
│   └── eval_tasks.yaml      # 评估任务配置
├── data/
│   ├── train_samples/       # 训练样本
│   ├── evaluation/          # 评估结果
│   └── weights/             # 模型权重
│       ├── steve1/          # STEVE-1 权重
│       ├── vpt/             # VPT 基础模型
│       └── mineclip/        # MineCLIP 权重
├── scripts/
│   ├── run_evaluation.sh    # 评估脚本
│   └── run_record_samples.sh # 样本录制脚本
├── src/
│   ├── evaluation/          # 评估框架
│   ├── envs/                # 环境封装
│   ├── utils/               # 工具函数
│   └── translation/         # 翻译模块
├── docs/                    # 文档
├── docker/                  # Docker 构建文件
├── Dockerfile               # Docker 镜像定义
└── requirements.txt         # Python 依赖
```

## 🚀 快速开始

### 环境要求

- **操作系统**：Linux (推荐) 或 macOS
- **Python**：3.9
- **Java**：JDK 8
- **GPU**：可选，支持 CUDA 或 Apple MPS

### 安装

详细安装步骤请参考 [部署文档](docs/guides/DEPLOYMENT.md)。

**快速安装（Docker 方式）**：
```bash
# 构建 Docker 镜像
docker build -t aimc:latest .

# 运行容器
docker run -it --rm -v $(pwd):/workspace aimc:latest
```

**本地安装（macOS）**：
```bash
# 创建 conda 环境
conda create -n minedojo-x86 python=3.9
conda activate minedojo-x86

# 安装依赖
pip install -r requirements.txt
pip install minedojo
pip install git+https://github.com/MineDojo/MineCLIP
```

### 下载模型权重

```bash
# STEVE-1 权重
mkdir -p data/weights/steve1
# 下载 steve1.weights 和 steve1_prior.pt 到 data/weights/steve1/

# VPT 权重
mkdir -p data/weights/vpt
# 下载 2x.model 和 2x.weights 到 data/weights/vpt/

# MineCLIP 权重
mkdir -p data/weights/mineclip
# 下载 attn.pth 到 data/weights/mineclip/
```

## 📊 评估系统

### 运行评估

```bash
# 评估单个任务
scripts/run_evaluation.sh --task harvest_1_log --n-trials 3

# 评估任务集
scripts/run_evaluation.sh --task-set baseline --n-trials 1 --enable_report

# 使用自定义配置
scripts/run_evaluation.sh --config config/eval_tasks.yaml --task-set quick_test
```

### 评估指标

| 指标 | 说明 |
|------|------|
| **① 目标嵌入基线** | MineCLIP 文本-视觉对齐基准 |
| **② Prior 目标嵌入** | Prior 输出与目标视觉的相似度 |
| **③ 接近率基线** | 专家演示的目标接近进度 |
| **④ Policy 接近率** | 模型执行的目标接近进度 |
| **⑤ Policy 成功率** | 任务完成率 |

### 任务类别

- **采集任务** (harvest)：采集木头、煤炭、花等
- **战斗任务** (combat)：攻击猪、牛、僵尸等
- **合成任务** (techtree)：制作木板、工具、熔炉等

## 🎬 录制训练样本

```bash
# 列出可用任务
scripts/run_record_samples.sh --list-tasks

# 录制样本（全屏模式推荐）
scripts/run_record_samples.sh --task harvest_1_log --fullscreen

# 录制完成后自动生成 visual_embeds.pkl
```

**控制说明**：
- 移动：W/A/S/D
- 跳跃：Space
- 攻击：F 或 鼠标左键
- 相机：鼠标移动
- 重录：Q
- 退出：ESC

## 📚 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                    EvaluationFramework                       │
│  (调度器：管理评估流程、生成报告)                              │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
┌───────────────┐    ┌───────────────┐    ┌───────────────┐
│ PriorEvaluator│    │PolicyEvaluator│    │ TaskLoader    │
│ (Prior 评估)  │    │ (Policy 评估) │    │ (任务配置)    │
└───────────────┘    └───────────────┘    └───────────────┘
        │                     │
        ▼                     ▼
┌───────────────┐    ┌───────────────┐
│   MineCLIP    │    │   STEVE-1     │
│ (视觉-文本)   │    │  (Policy)     │
└───────────────┘    └───────────────┘
                              │
                              ▼
                    ┌───────────────┐
                    │ MineRL/MineDojo│
                    │   (环境)      │
                    └───────────────┘
```

## 🔗 相关项目

- [STEVE-1](https://github.com/Shalev-Lifshitz/STEVE-1) - 原始 STEVE-1 项目
- [MineCLIP](https://github.com/MineDojo/MineCLIP) - 视觉-语言模型
- [MineDojo](https://github.com/MineDojo/MineDojo) - Minecraft 研究框架
- [MineRL](https://github.com/minerllabs/minerl) - Minecraft 强化学习环境

## 📖 文档

- [部署指南](docs/guides/DEPLOYMENT.md) - 完整安装步骤
- [评估框架指南](docs/guides/EVALUATION_FRAMEWORK_GUIDE.md) - 评估系统详解
- [任务配置指南](docs/guides/TASK_WRAPPERS_GUIDE.md) - 自定义任务

## 📝 License

MIT License

